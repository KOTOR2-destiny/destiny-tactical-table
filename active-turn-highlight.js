// Player and GM active-turn indicator. Read-only; GM controls remain in combat-turns.js.
import {createClient} from 'https://esm.sh/@supabase/supabase-js@2.105.0';
const db=createClient('https://zeyvkuhqgbqjqalxubrq.supabase.co','sb_publishable_trF3YpEfBC7rMZpxs0lzzg_utxyNU7t');
const layer=document.getElementById('tokenLayer'),order=document.getElementById('combatOrder'),role=document.getElementById('roleLabel');
const banner=document.createElement('div');banner.id='activeTurnBanner';banner.setAttribute('role','status');banner.setAttribute('aria-live','polite');banner.style.cssText='padding:10px 12px;margin:8px 0;border:1px solid #4f9fbd;border-radius:8px;background:rgba(24,91,119,.3);color:#d8f5ff;font-weight:700;letter-spacing:.04em';banner.textContent='COMBAT NOT STARTED';order.parentElement.insertBefore(banner,order);
const css=document.createElement('style');css.textContent='#combatOrder .combat-row.turn-active{background:rgba(80,191,237,.32)!important;outline:2px solid #83e5ff!important;box-shadow:inset 4px 0 #83e5ff;border-radius:5px}#tokenLayer .token.turn-active-token{outline:3px solid #83e5ff!important;box-shadow:0 0 0 5px rgba(44,188,242,.3),0 0 22px rgba(44,188,242,.75)!important}';document.head.appendChild(css);
let sceneId=null,scene=null,channel=null,connecting=false,request=0;
function tokenEntries(){return [...layer.querySelectorAll('.token[data-id]')].map(el=>({id:el.dataset.id,name:el.querySelector('.token-name')?.textContent?.trim()||'',el}));}
function paint(){const entries=tokenEntries(),round=Number(scene?.combat_round||0),active=round>0?entries.find(t=>t.id===scene?.combat_active_token):null;banner.textContent=round>0?`ROUND ${round}  ·  ${active?`NOW ACTING: ${active.name}`:'ACTIVE COMBATANT NOT ON THIS MAP'}`:'COMBAT NOT STARTED';for(const t of entries)t.el.classList.toggle('turn-active-token',!!active&&t.id===active.id);for(const row of order.querySelectorAll('.combat-row')){const name=row.querySelector('strong')?.textContent?.trim();row.classList.toggle('turn-active',!!active&&name===active.name);if(active&&name===active.name)row.setAttribute('aria-current','step');else row.removeAttribute('aria-current');}}
async function refresh(){if(!sceneId)return;const id=sceneId;const {data,error}=await db.from('scenes').select('id,combat_round,combat_active_token').eq('id',id).single();if(!error&&id===sceneId){scene=data;paint();}}
async function connect(){if(connecting)return;const first=layer.querySelector('.token[data-id]');if(!first){paint();return;}connecting=true;const n=++request;try{const {data,error}=await db.from('scene_tokens').select('scene_id').eq('id',first.dataset.id).single();if(error||n!==request||!data)return;if(sceneId!==data.scene_id){if(channel){await db.removeChannel(channel);channel=null;}sceneId=data.scene_id;scene=null;channel=db.channel(`active-turn-highlight:${sceneId}`).on('postgres_changes',{event:'UPDATE',schema:'public',table:'scenes',filter:`id=eq.${sceneId}`},payload=>{if(payload.new.id===sceneId){scene=payload.new;paint();}}).subscribe();await refresh();}else paint();}finally{connecting=false;}}
new MutationObserver(()=>{connect();paint();}).observe(layer,{childList:true});new MutationObserver(()=>queueMicrotask(paint)).observe(order,{childList:true});new MutationObserver(paint).observe(role,{childList:true,subtree:true,characterData:true});document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh();});window.addEventListener('focus',refresh);setInterval(()=>{if(!document.hidden&&sceneId)refresh();},12000);connect();